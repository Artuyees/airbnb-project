import React from "react";

type Props = {
  properties: {
    name: string;
    icon: string;
  }[];
};
const HotelProperties = (props: Props) => {
  return <div>HotelProperties</div>;
};

export default HotelProperties;
